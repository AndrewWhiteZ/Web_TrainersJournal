import { BeltLevelColorPipe } from './belt-level-color.pipe';

describe('BeltLevelColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BeltLevelColorPipe();
    expect(pipe).toBeTruthy();
  });
});
