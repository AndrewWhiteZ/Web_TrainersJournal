import { BeltLevelPipe } from './belt-level.pipe';

describe('BeltLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new BeltLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
